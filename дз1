#include <iostream>
#include <cmath>
int main() {
  long long x;
  long long k;
  long long y=1;
  std::cin>>x;
  for (int i=63; i>=0; i--)
    {
      k=pow(2, i);
      y=(x&k)/k;
      std::cout<<y;
    } 
}
